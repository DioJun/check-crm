{
  "name": "checkmate-crm",
  "version": "2.0.0",
  "description": "Checkmate CRM - Gestão de Leads Desktop",
  "author": "Checkmate CRM",
  "license": "MIT",
  "main": "electron/main.js",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:electron": "npx wait-on http://localhost:5173 && electron .",
    "start": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\" \"npx wait-on http://localhost:5173 && electron .\""
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "electron": "^31.0.0",
    "wait-on": "^9.0.4"
  }
}
